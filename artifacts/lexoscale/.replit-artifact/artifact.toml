kind = "web"
previewPath = "/"
title = "LexoScale"
version = "1.0.0"
id = "artifacts/lexoscale"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22235

[services.development]
run = "pnpm --filter @workspace/lexoscale run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lexoscale", "run", "build" ]
publicDir = "artifacts/lexoscale/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22235"
BASE_PATH = "/"
